{
  "name": "life-script",
  "version": "1.0.1",
  "description": "organizador de la vida basado en un video de linux-chad",
  "license": "ISC",
  "author": "Gabriel Farías",
  "type": "commonjs",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "build": "esbuild index.js --bundle --minify --platform=node --outfile=dist/bundle.js && chmod +x dist/bundle.js && dpkg-buildpackage -us -uc && mv ../life-script_* ./dist/",
    "test": "LIFE_DIR=./test/temp-life/ node --test ./test/index.test.js" 
  },
  "devDependencies": {
    "esbuild": "0.27.1"
  }
}
